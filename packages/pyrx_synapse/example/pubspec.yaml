name: pyrx_synapse_example
description: >-
  Minimal pub.dev-rendered example of pyrx_synapse. For the full
  Flutter sample app see examples/synapse_flutter_demo/ at the repo
  root.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  pyrx_synapse:
    path: ..

dev_dependencies:
  flutter_lints: ^4.0.0
